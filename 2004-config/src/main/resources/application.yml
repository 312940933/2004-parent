server:
  port: 9000
spring:
  application:
    name: 2004-config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:properties/
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka